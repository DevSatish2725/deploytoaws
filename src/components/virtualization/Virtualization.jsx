import SelfMade from "./SelfMade";
const Virtualization = () => {
  return (
      <div>
      <SelfMade />
      {/* <List
        rowComponent={Row}
        rowCount={names.length}
        rowHeight={25}
        rowProps={{
          names: names,
        }}
      /> */}
    </div>
  );
};

export default Virtualization;
